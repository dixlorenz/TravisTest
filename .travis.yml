sudo: required

branches:
  only:
    - master

before_install: 

install: 

language: objective-c
osx_image: xcode10

script:
  - set -o pipefail && xcodebuild -project TravisTest.xcodeproj INSTALL_PATH=$HOME/bin DSTROOT=/ -configuration Release -scheme TravisTest install | xcpretty

deploy:
  skip_cleanup: true
  provider: s3
  access_key_id: AKIAICYRW6GNSXSSN3WQ
  secret_access_key: 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
  bucket: medimach
  region: eu-west-1
  local_dir: $HOME/bin
  upload-dir: builds/latest
  acl: authenticated_read
